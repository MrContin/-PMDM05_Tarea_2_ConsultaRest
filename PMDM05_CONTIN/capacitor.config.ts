import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PMDM05_CONTIN',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
